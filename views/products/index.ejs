<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>All Products</h1>
    <div>
      <% for (let product of products) { %>
        <p><%= product.title %></p>
        <p><%= product.description %></p>
        <p>$<%= product.price %></p>
      <% } %>
    </div>
  </body>
</html>
