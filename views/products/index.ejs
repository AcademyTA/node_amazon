<%- include('../partials/header') %>

<h1>All Products</h1>
<div>
  <% for (let product of products) { %>
    <p><%= product.title %></p>
    <p><%= product.description %></p>
    <p>$<%= product.price %></p>
  <% } %>
</div>

<%- include('../partials/footer') %>
